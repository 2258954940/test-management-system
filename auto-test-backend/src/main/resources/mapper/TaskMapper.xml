<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auto.test.mapper.TaskMapper">
    <!-- 查询已完成的任务 -->
    <select id="selectFinishedTasks" resultType="com.auto.test.entity.Task">
        SELECT 
            id, 
            task_name, 
            case_id, -- 关键：把case_ids改成case_id（匹配数据库字段）
            exec_type, 
            cron_expression, 
            status, 
            create_by, 
            create_time
        FROM test_task
        WHERE status = 'finished' 
        ORDER BY create_time DESC
    </select>
</mapper>